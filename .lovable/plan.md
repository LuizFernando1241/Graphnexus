Trocar o valor default do sort em `useTasksView.ts` de `"manual"` para `"created"`. Isso faz com que o sort "Mais recentes" seja o padrão ao acessar a página de tarefas, garantindo que tarefas mais recentes fiquem no topo de forma explícita e visível no seletor de ordenação.